name: Ramadan Series Scraper

on:
  workflow_dispatch:  # تشغيل يدوي
  schedule:
    # تشغيل كل ساعة
    - cron: "0 * * * *"  # كل ساعة في الدقيقة 0

jobs:
  extract-ramadan-series:
    runs-on: ubuntu-latest
    
    # حل الصلاحيات - السماح بكتابة إلى المستودع
    permissions:
      contents: write  # هذا مهم!
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          # إضافة هذه الإعدادات للسماح بالدفع من نفس المستودع
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          
      - name: Install dependencies
        run: |
          npm install jsdom
          echo "✅ تم تثبيت الحزم المطلوبة"

      - name: Create Ramadan directory
        run: |
          # إنشاء مجلد Ramadan/2025 مسبقاً
          mkdir -p Ramadan/2025
          echo "📁 تم إنشاء مجلد Ramadan/2025"

      - name: Run Ramadan.js
        run: |
          echo "🎬 بدء استخراج مسلسلات رمضان..."
          echo "📅 السنة: 2025"
          echo "⏰ الوقت: $(date)"
          echo "🔄 الجدولة: كل ساعة"
          echo "📁 الملف: Ramadan.js"
          echo "🔄 جاري التشغيل..."
          
          # تشغيل ملف Ramadan.js
          if [ -f "Ramadan.js" ]; then
            node Ramadan.js
            EXIT_CODE=$?
            if [ $EXIT_CODE -eq 0 ]; then
              echo "✅ تم تشغيل Ramadan.js بنجاح"
            else
              echo "❌ Ramadan.js خرج برمز خطأ: $EXIT_CODE"
              exit $EXIT_CODE
            fi
          else
            echo "❌ ملف Ramadan.js غير موجود!"
            echo "📂 الملفات الموجودة:"
            ls -la *.js || echo "لا توجد ملفات js"
            exit 1
          fi
          
          echo "✅ تم الانتهاء من استخراج مسلسلات رمضان"

      - name: Check results
        run: |
          echo "📊 ====== نتائج استخراج مسلسلات رمضان ======"
          echo "⏰ وقت التشغيل: $(date)"
          echo "🔄 التكرار: كل ساعة"
          echo "📅 السنة: 2025"
          
          # التحقق من وجود ملف index_2025.json
          if [ -f "Ramadan/2025/index_2025.json" ]; then
            echo "✅ تم إنشاء/تحديث ملف index_2025.json"
            
            # تثبيت jq إذا لم يكن مثبتاً
            if ! command -v jq &> /dev/null; then
              echo "📦 تثبيت jq لعرض البيانات..."
              sudo apt-get update && sudo apt-get install -y jq
            fi
            
            # عرض معلومات الفهرس
            echo ""
            echo "📋 معلومات الفهرس:"
            if jq -e '.totalSeries' Ramadan/2025/index_2025.json > /dev/null 2>&1; then
              TOTAL_SERIES=$(jq -r '.totalSeries' Ramadan/2025/index_2025.json)
              TOTAL_EPISODES=$(jq -r '.totalEpisodes' Ramadan/2025/index_2025.json)
              SCRAPED_AT=$(jq -r '.scrapedAt' Ramadan/2025/index_2025.json)
              
              echo "🎯 إجمالي المسلسلات: $TOTAL_SERIES"
              echo "🎬 إجمالي الحلقات: $TOTAL_EPISODES"
              echo "⏰ وقت الاستخراج: $SCRAPED_AT"
              
              # عرض أول 3 مسلسلات
              echo ""
              echo "📺 أول 3 مسلسلات مستخرجة:"
              jq -r '.series[0:3] | .[] | "  - \(.title) (ID: \(.id)) | الحلقات: \(.episodes) | الملف: \(.fileName)"' Ramadan/2025/index_2025.json
              
              # حساب عدد الملفات المسلسلات المنشأة
              SERIES_FILES_COUNT=$(ls -1 Ramadan/2025/*.json 2>/dev/null | grep -v index_2025.json | wc -l)
              echo ""
              echo "📁 الملفات المنشأة: $SERIES_FILES_COUNT ملف مسلسل"
              
            else
              echo "⚠️ هيكل JSON غير صالح أو لا يحتوي على البيانات المتوقعة"
            fi
            
            # عرض حجم مجلد Ramadan/2025
            echo ""
            echo "💾 حجم المجلد:"
            du -sh Ramadan/2025/
            
          else
            echo "❌ لم يتم إنشاء index_2025.json"
            echo "📂 محتويات مجلد Ramadan/2025:"
            if [ -d "Ramadan/2025" ]; then
              ls -la Ramadan/2025/
            else
              echo "❌ مجلد Ramadan/2025 غير موجود"
            fi
          fi

      - name: Commit and push changes
        run: |
          # تكوين Git
          git config --global user.name "GitHub Actions Bot"
          git config --global user.email "actions@github.com"
          
          # إضافة جميع ملفات المسلسلات
          echo "🔄 إضافة ملفات المسلسلات..."
          git add Ramadan/ || echo "⚠️ لا توجد ملفات جديدة"
          
          # التحقق إذا كانت هناك تغييرات
          if git diff --cached --quiet; then
            echo "📭 لا توجد تغييرات جديدة للرفع"
          else
            echo "🔼 رفع التغييرات..."
            
            # جلب معلومات للتحديث
            if [ -f "Ramadan/2025/index_2025.json" ]; then
              TOTAL_SERIES=$(jq -r '.totalSeries // 0' Ramadan/2025/index_2025.json 2>/dev/null || echo 0)
              TOTAL_EPISODES=$(jq -r '.totalEpisodes // 0' Ramadan/2025/index_2025.json 2>/dev/null || echo 0)
              TIMESTAMP=$(date +'%Y-%m-%d %H:%M')
              
              git commit -m "🎬 تحديث مسلسلات رمضان 2025 - $TIMESTAMP" \
                -m "تحديث تلقائي كل ساعة" \
                -m "عدد المسلسلات: $TOTAL_SERIES" \
                -m "عدد الحلقات: $TOTAL_EPISODES"
                
              echo "✅ تم عمل commit"
            else
              git commit -m "🎬 تحديث مسلسلات رمضان - $(date +'%Y-%m-%d %H:%M')" \
                -m "تحديث تلقائي كل ساعة"
            fi
            
            # push التغييرات
            git pull --rebase origin main
            git push origin HEAD:main
            echo "✅ تم رفع التغييرات بنجاح"
          fi

      - name: Create log file
        run: |
          # إنشاء ملف سجل
          TIMESTAMP=$(date +'%Y-%m-%d %H:%M:%S')
          LOG_FILE="Ramadan/2025/scrape_log.json"
          
          # تكوين Git
          git config --global user.name "GitHub Actions Bot"
          git config --global user.email "actions@github.com"
          
          # قراءة معلومات الفهرس
          if [ -f "Ramadan/2025/index_2025.json" ]; then
            TOTAL_SERIES=$(jq -r '.totalSeries // 0' Ramadan/2025/index_2025.json 2>/dev/null || echo 0)
            TOTAL_EPISODES=$(jq -r '.totalEpisodes // 0' Ramadan/2025/index_2025.json 2>/dev/null || echo 0)
            
            # حساب عدد ملفات المسلسلات
            SERIES_FILES_COUNT=$(ls -1 Ramadan/2025/*.json 2>/dev/null | grep -v index_2025.json | grep -v scrape_log.json | wc -l)
            
            # قراءة البيانات الحالية أو إنشاء مصفوفة فارغة
            if [ -f "$LOG_FILE" ]; then
              # إضافة سجل جديد
              jq --arg timestamp "$TIMESTAMP" \
                 --argjson total_series "$TOTAL_SERIES" \
                 --argjson total_episodes "$TOTAL_EPISODES" \
                 --argjson series_files "$SERIES_FILES_COUNT" \
                 '. += [{
                   "timestamp": $timestamp,
                   "total_series": $total_series,
                   "total_episodes": $total_episodes,
                   "series_files": $series_files,
                   "year": "2025"
                 }]' \
                 "$LOG_FILE" > "$LOG_FILE.tmp" && mv "$LOG_FILE.tmp" "$LOG_FILE"
            else
              # إنشاء ملف سجل جديد
              echo '[{
                "timestamp": "'$TIMESTAMP'",
                "total_series": '$TOTAL_SERIES',
                "total_episodes": '$TOTAL_EPISODES',
                "series_files": '$SERIES_FILES_COUNT',
                "year": "2025"
              }]' > "$LOG_FILE"
            fi
            
            # إضافة ملف السجل إلى Git
            git add "$LOG_FILE"
            git commit -m "📝 تحديث سجل رمضان - $TIMESTAMP" || echo "⚠️ لا توجد تغييرات في السجل"
            
            # push إذا كانت هناك تغييرات
            if ! git diff --cached --quiet; then
              git pull --rebase origin main
              git push origin HEAD:main
              echo "✅ تم تحديث سجل التشغيل"
            else
              echo "📝 لا توجد تغييرات في السجل للرفع"
            fi
            
          fi

      - name: Send notification
        if: always()
        run: |
          if [ "${{ job.status }}" == "success" ]; then
            echo "✅ تم استخراج مسلسلات رمضان 2025 بنجاح!"
            echo "⏰ الوقت: $(date)"
            echo "📁 الموقع: Ramadan/2025/"
            echo "🎬 تم تحديث جميع المسلسلات"
          else
            echo "❌ فشل استخراج مسلسلات رمضان!"
            echo "⏰ الوقت: $(date)"
            echo "🔧 الرجاء التحقق من الأخطاء أعلاه"
          fi
