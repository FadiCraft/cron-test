name: KiroZozo Movie Extractor

on:
  workflow_dispatch:
  schedule:
    - cron: "*/30 * * * *"  # كل 30 دقيقة

jobs:
  extract-movies:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          
      - name: Install dependencies
        run: |
          npm init -y
          npm install jsdom
          echo "✅ تم تثبيت الحزم"

      - name: Run movie scraper
        run: |
          echo "🚀 بدء عملية الاستخراج المتقدمة..."
          echo "📅 الوقت: $(date)"
          
          # تشغيل ملف fetch.js الموجود في الريبو
          node fetch.js
          
          echo "📁 عرض هيكل المجلدات:"
          ls -la
          echo ""
          echo "📂 محتويات مجلد movies:"
          if [ -d "movies" ]; then
            ls -la movies/
            echo ""
            echo "🎬 عدد ملفات الأفلام: $(ls movies/*.json 2>/dev/null | wc -l || echo '0')"
          else
            echo "❌ مجلد movies غير موجود"
          fi

      - name: Show results summary
        run: |
          echo "📊 ملخص النتائج:"
          if [ -f "result.json" ]; then
            cat result.json
          fi
          
          echo ""
          echo "📋 الفهرس:"
          if [ -f "movies/index.json" ]; then
            cat movies/index.json | jq '. | {totalMovies: .totalMovies, scrapedMovies: .scrapedMovies, lastUpdated: .lastUpdated}'
          fi

      - name: Commit changes
        run: |
          git config user.name "KiroZozo Bot"
          git config user.email "bot@kirozozo.com"
          git add .
          git commit -m "🎬 تحديث الأفلام $(date +'%Y-%m-%d %H:%M')" || echo "لا توجد تغييرات"
          git push || echo "لا شيء للرفع"
