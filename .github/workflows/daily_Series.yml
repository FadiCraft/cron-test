name: KiroZozo Series Extractor

on:
  workflow_dispatch:
  schedule:
    - cron: "0 2 * * *"

jobs:
  extract-series:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          
      - name: Install dependencies
        run: |
          npm install jsdom
          echo "✅ تم تثبيت الحزم"

      - name: Create series directories
        run: |
          mkdir -p Series/Series
          mkdir -p Series/Seasons
          mkdir -p Series/Episodes
          echo "📁 تم إنشاء مجلدات Series"

      - name: Run the series scraper
        run: |
          echo "🚀 بدء عملية استخراج المسلسلات..."
          echo "📅 الوقت: $(date)"
          node daily_Series.js
          echo "✅ تم الانتهاء من استخراج المسلسلات"

      - name: Configure Git
        run: |
          git config user.name "GitHub Actions Bot"
          git config user.email "actions@github.com"

      - name: Pull latest changes
        run: |
          git pull origin main --rebase || echo "⚠️ لا يوجد تغييرات للسحب"

      - name: Add and commit changes
        run: |
          git add Series/ daily_Series.js || echo "⚠️ لا توجد ملفات جديدة"
          
          if git diff --cached --quiet; then
            echo "📭 لا توجد تغييرات للرفع"
          else
            git commit -m "📺 تحديث المسلسلات - $(date +'%Y-%m-%d %H:%M')" -m "تم استخراج المسلسلات تلقائياً"
            echo "✅ تم commit التغييرات"
          fi

      - name: Push changes
        run: |
          if git log origin/main..HEAD --oneline | grep -q .; then
            echo "🔼 رفع التغييرات..."
            git push origin main
            echo "✅ تم رفع التغييرات"
          else
            echo "📭 لا توجد تغييرات للرفع"
          fi
