name: Extract MBC Channels

on:
  schedule:
    # يشغل يومياً في 2 صباحاً (توقيت UTC)
    - cron: '0 2 * * *'
  workflow_dispatch:  # يسمح بالتشغيل اليدوي
  push:
    branches: [ main ]

jobs:
  extract-channels:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout repository
      uses: actions/checkout@v3
      
    - name: ⎔ Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'
        cache: 'npm'
        
    - name: 📦 Install dependencies
      run: npm ci
      
    - name: 🚀 Extract channels
      run: npm start
      
    - name: 📊 Commit and push changes
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add TV/tv.json TV/index.html
        git diff --quiet && git diff --staged --quiet || git commit -m "📺 Auto-update MBC channels [skip ci]"
        git push
