name: Extract MBC Channels

on:
  schedule:
    - cron: '0 2 * * *'
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  extract-channels:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout repository
      uses: actions/checkout@v3
      
    - name: ⎔ Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'
        cache: 'npm'
        
    - name: 📦 Install dependencies
      run: npm ci
      
    - name: 🚀 Extract channels
      run: node tv.js  # ⬅️ غير من npm start إلى node tv.js مباشرة
      
    - name: 🔍 Debug - Check if files created
      run: |
        echo "=== Checking files ==="
        ls -la
        echo "=== Checking TV directory ==="
        ls -la TV/ || echo "TV directory not found!"
        
    - name: 📊 Commit and push changes
      run: |
        # تأكد أولاً أن الملفات موجودة
        if [ -f "TV/tv.json" ]; then
          echo "✅ tv.json exists, proceeding with commit..."
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add TV/
          git diff --quiet && git diff --staged --quiet || git commit -m "📺 Auto-update MBC channels [skip ci]"
          git push
        else
          echo "❌ tv.json not found! Skipping commit."
          exit 1
        fi
